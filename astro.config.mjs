import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://mi-camino-santiago.vercel.app', // Cambia esto cuando tengas tu dominio o deja así por ahora
});
