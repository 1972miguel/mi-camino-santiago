---
// src/layouts/PostLayout.astro
import MainLayout from './MainLayout.astro';
import NavBar from '../components/NavBar.astro';

const { frontmatter } = Astro.props;
---

<MainLayout title={frontmatter.title}>
  <NavBar /> <article class="post-container">
    <header class="post-header">
      <h1>{frontmatter.title}</h1>
      <p class="post-date">{frontmatter.dateFormatted}</p>
      {frontmatter.image && <img src={frontmatter.image} alt={frontmatter.title} class="hero-image" />}
    </header>

    <div class="markdown-content">
      <slot />
    </div>
  </article>
</MainLayout>

<style>
.post-container {
    max-width: 900px;
    margin: 0 auto;
    /* Reducimos el padding superior en móviles de 8rem a 5rem */
    padding: 6rem 1.5rem; 
  }

  /* --- SOLUCIÓN PARA LA INTERFAZ DE IMÁGENES --- */

  /* Forzamos que el contenedor del Lightbox no sea más ancho que el contenido del post */
  :global(.glightbox-container) {
    max-width: 1100px; /* Un poco más ancho que tu post para dar aire */
    margin: 0 auto;
    left: 50% !important;
    transform: translateX(-50%);
  }

  /* Ajustamos la posición de la X para que no se escape a la esquina de la pantalla */
  :global(.gclose) {
    background-color: #2d5a27 !important; /* Tu verde oscuro */
    width: 42px !important;
    height: 42px !important;
    border-radius: 50% !important;
    opacity: 1 !important;
    display: flex !important;
    align-items: center;
    justify-content: center;
    box-shadow: 0 4px 15px rgba(0,0,0,0.4) !important;
    
    /* La posicionamos de forma relativa al contenedor centrado */
    top: 15px !important;
    right: 15px !important;
  }

  :global(.gclose svg path) {
    fill: white !important;
  }

  /* Estilos del contenido */
  .post-header {
    text-align: center;
    margin-bottom: 3rem;
  }

  .post-header h1 {
    font-size: clamp(1.8rem, 5vw, 2.2rem);
    color: var(--dark-green);
    margin-bottom: 0.5rem;
  }

  .post-date {
    color: var(--dark-green);
    font-weight: bold;
    font-size: clamp(0.9rem, 2vw, 1.1rem);
  }

  .hero-image {
    width: 100%;
    border-radius: 20px;
    margin-top: 2rem;
    box-shadow: 0 8px 25px rgba(0,0,0,0.1);
  }

  /* ESTILOS PARA EL CONTENIDO DEL MARKDOWN */
  .markdown-content :global(h1) {
    color: var(--dark-green);
    font-size: clamp(1.5rem, 4vw, 1.9rem);
    margin-top: 2rem;
    margin-bottom: 1rem;
  }

  .markdown-content :global(p) {
    line-height: 1.8;
    color: var(--text-dark);
    margin-bottom: 1.5rem;
    font-size: clamp(1rem, 1vw + 0.5rem, 1.4rem);
  }

  .markdown-content :global(h3) {
    color: var(--light-green);
    border-bottom: 2px solid var(--light-green);
    display: inline-block;
    margin-top: 2rem;
    margin-bottom: 1rem;
  }

  .markdown-content :global(ul) {
    margin-left: 1.5rem;
    margin-bottom: 1.5rem;
  }

  .markdown-content :global(li) {
    margin-bottom: 0.5rem;
    list-style: disc;
  }
  @media (max-width: 768px) {
    .post-container {
      padding: 5rem 1rem;
    }
    
    .post-header h1 {
      /* Usamos el valor mínimo de clamp para que no se vea gigante */
      font-size: 1.8rem; 
    }

    .hero-image {
      border-radius: 12px; /* Bordes más sutiles en pantallas pequeñas */
      margin-top: 1.5rem;
    }
  }
</style>